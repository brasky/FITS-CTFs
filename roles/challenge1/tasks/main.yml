- name: Pull alpine
  docker_image:
    name: alpine
    source: pull

- name: Run the container
  docker_container:
    name: challenge1-ssh
    image: alpine:latest
    state: started
    command: touch /tmp/test

