- name: Create a directory if it does not exist
  file:
    path: /tmp/docker-challenge9
    state: directory
    mode: '0755'

- name: Copy dockerfile
  copy:
      src: "{{ repo_path }}/roles/challenge9/docker/Dockerfile"
      dest: /tmp/docker-challenge9/Dockerfile
      mode: '0744'
      force: yes

- name: Copy entrypoint script
  copy:
      src: "{{ repo_path }}/roles/challenge9/resources/entrypoint.sh"
      dest: /tmp/docker-challenge9/entrypoint.sh
      mode: '0744'
      force: yes

- name: Build image
  docker_image:
    name: challenge9-image
    build:
      path: /tmp/docker-challenge9/
    source: build
    state: present
    force_source: yes
    repository: challenge9-image:latest
    force_tag: yes

- name: Run the container
  docker_container:
    name: challenge9-ssh
    image: challenge9-image
    state: started
    restart: yes
    expose: 22
    detach: yes
    published_ports: '6009:22'
